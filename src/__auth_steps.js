/**
 * 1. create a new project in console.firebase.google.com 
 * 2 npm install firebase
 * 3. create firebase.init.js and import getAuth to export auth
 * 4. Firebase settings -> Authentication -> enable email and password auth
 * 5. create Login, Signup component, setup route
 * 6. attached from field handler and form submit handler. 
 * 7. npm install --save react-firebase-hooks (it's taken from react firebase hooks).
 * 8. useCreateUserWithEmailAndPassword from react-firebase-hook
 */

/**
 * Hosing steps.
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 3. firebase init
 * 4. firebase deploy
 */